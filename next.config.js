/** @type {import('next').NextConfig} */
const nextConfig = {
  // Public folder files are already served at root paths
  // So public/index.html is accessible at /index.html
  // The pages/index.tsx redirects / to /index.html
};

module.exports = nextConfig;

